Poniżej poprawiona wersja jako **zamknięty plan v0.1**. Trzymamy główną ideę projektu z notatki: mały lab, local-first, cloud-ready, pokazujący delivery flow przez GitOps i rollback przez `git revert`.

# GitOps Cloud Lab — v0.1 plan zamknięty ✅

## Decyzje zamknięte

| Obszar         | Decyzja                                      |
| -------------- | -------------------------------------------- |
| Local cluster  | **kind**                                     |
| GitOps update  | **GitHub Actions + `yq` update `image.tag`** |
| CD tool        | **Argo CD tylko obserwuje Git**              |
| Env            | tylko **dev**                                |
| Cloud          | **brak `infra/aws/` w v0.1**                 |
| App endpoints  | `/health`, `/ready`, `/version`              |
| Metrics        | poza v0.1, wraca w v0.5                      |
| Prod/promotion | poza v0.1, wraca w v0.3                      |
| Case study     | piszemy na końcu, nie w Slice 0              |

---

# 1. MVP scope 🎯

## Cel v0.1

Udowodnić flow:

```text
code change
→ GitHub Actions CI
→ Docker image build
→ push image to GHCR
→ yq update image.tag in gitops/envs/dev/values.yaml
→ commit + push GitOps change
→ Argo CD sync
→ Kubernetes deployment on kind
→ rollback by git revert
```

## Success metric v0.1

```text
Zmieniam image.tag w gitops/envs/dev/values.yaml
przez push kodu → CI → build → yq update.

Argo CD pokazuje Synced + Healthy w ~3 min.

curl /version zwraca nowy tag.

Zero ręcznego kubectl apply albo helm upgrade dla deploymentu aplikacji.
```

**Uwaga praktyczna:** `kubectl` może być użyte do bootstrapu klastra/Argo CD. Nie używamy go jako metody deployowania `demo-api`.

---

# 2. Repo structure v0.1 📁

To jest wersja oczyszczona:

```text
gitops-cloud-lab/
├── apps/
│   └── demo-api/
│       ├── app/
│       │   ├── main.py
│       │   └── version.py
│       ├── tests/
│       │   └── test_health.py
│       ├── Dockerfile
│       ├── requirements.txt
│       └── README.md
│
├── deploy/
│   └── helm/
│       └── demo-api/
│           ├── Chart.yaml
│           ├── values.yaml
│           └── templates/
│               ├── deployment.yaml
│               └── service.yaml
│
├── gitops/
│   ├── apps/
│   │   └── demo-api-application.yaml
│   └── envs/
│       └── dev/
│           └── values.yaml
│
├── infra/
│   └── local/
│       ├── main.tf
│       ├── providers.tf
│       ├── variables.tf
│       └── README.md
│
├── docs/
│   ├── ARCHITECTURE.md
│   ├── RUNBOOK_DEPLOY.md
│   ├── RUNBOOK_ROLLBACK.md
│   └── ADR/
│       └── 0001-local-first-kind-gitops.md
│
├── .github/
│   └── workflows/
│       ├── ci.yml
│       ├── image-build.yml
│       └── gitops-update.yml
│
├── Makefile
└── README.md
```

## Celowo usunięte z v0.1

```text
❌ infra/aws/
❌ gitops/envs/prod/
❌ /metrics endpoint
❌ docs/CASE_STUDY.md jako early task
❌ Argo CD Image Updater
```

---

# 3. Pierwszy commit ✅

Pierwszy commit ma zamknąć tylko fundament projektu.

## Commit message

```bash
chore: initialize gitops cloud lab structure
```

## Komendy

```bash
mkdir gitops-cloud-lab
cd gitops-cloud-lab

git init

mkdir -p apps/demo-api/app
mkdir -p apps/demo-api/tests
mkdir -p deploy/helm/demo-api/templates
mkdir -p gitops/apps
mkdir -p gitops/envs/dev
mkdir -p infra/local
mkdir -p docs/ADR
mkdir -p .github/workflows

touch README.md
touch Makefile

touch apps/demo-api/README.md

touch deploy/helm/demo-api/Chart.yaml
touch deploy/helm/demo-api/values.yaml

touch gitops/apps/demo-api-application.yaml
touch gitops/envs/dev/values.yaml

touch infra/local/main.tf
touch infra/local/providers.tf
touch infra/local/variables.tf
touch infra/local/README.md

touch docs/ARCHITECTURE.md
touch docs/RUNBOOK_DEPLOY.md
touch docs/RUNBOOK_ROLLBACK.md
touch docs/ADR/0001-local-first-kind-gitops.md

touch .github/workflows/ci.yml
touch .github/workflows/image-build.yml
touch .github/workflows/gitops-update.yml

git add .
git commit -m "chore: initialize gitops cloud lab structure"
```

## Minimalny `README.md`

```md
# GitOps Cloud Lab

Small local-first DevOps lab showing Kubernetes application delivery with GitOps.

## Goal

Prove the workflow:

Code change → CI → image build → GitOps values update → Argo CD sync → Kubernetes deployment → rollback by git revert.

## Stack

- kind
- GitHub Actions
- Docker
- GHCR
- Helm
- Argo CD
- Terraform

## MVP success metric

A code change triggers CI, builds a Docker image, updates `gitops/envs/dev/values.yaml` with `yq`, Argo CD syncs the change, and `/version` returns the new image tag.

No manual `kubectl apply` or `helm upgrade` is used for application deployment.
```

---

# 4. GitHub issues — v0.1 milestone 🧩

Milestone:

```text
v0.1.0 - Local GitOps MVP
```

## Issue #1 — Initialize project structure

**Cel:** przygotować czyste repo pod v0.1.

**Zakres:**

```text
README.md
docs/ARCHITECTURE.md
docs/ADR/0001-local-first-kind-gitops.md
foldery v0.1
```

**PASS:**

```text
✅ Repo ma apps/, deploy/, gitops/, infra/local/, docs/, .github/
✅ Nie ma infra/aws/
✅ Nie ma gitops/envs/prod/
✅ README opisuje cel v0.1
✅ ADR zamyka decyzję: kind + local-first + cloud-ready later
✅ Commit: chore: initialize gitops cloud lab structure
```

---

## Issue #2 — Add demo-api

**Cel:** mała aplikacja do pokazania deploymentu.

**Endpointy:**

```text
GET /health
GET /ready
GET /version
```

**PASS:**

```text
✅ App uruchamia się lokalnie
✅ /health zwraca 200
✅ /ready zwraca 200
✅ /version zwraca service, version, environment, commit/tag
✅ Nie ma /metrics w v0.1
✅ Testy przechodzą lokalnie
```

---

## Issue #3 — Add Docker support

**Cel:** aplikacja działa jako kontener.

**PASS:**

```text
✅ Dockerfile istnieje w apps/demo-api/
✅ docker build działa
✅ docker run działa
✅ curl /health z kontenera zwraca 200
✅ Image może przyjąć tag typu sha-<commit>
```

---

## Issue #4 — Add CI workflow

**Cel:** GitHub Actions sprawdza aplikację przed buildem image.

**PASS:**

```text
✅ .github/workflows/ci.yml istnieje
✅ Workflow odpala się na push i PR
✅ Instaluje dependencies
✅ Uruchamia testy
✅ Błędny test blokuje pipeline
```

---

## Issue #5 — Build and push image to GHCR

**Cel:** pipeline buduje image i publikuje go do GHCR.

**PASS:**

```text
✅ .github/workflows/image-build.yml istnieje
✅ Image buduje się po przejściu testów
✅ Image trafia do GHCR
✅ Tag zawiera commit SHA
✅ Image można pobrać przez docker pull
```

---

## Issue #6 — Add Helm chart for demo-api

**Cel:** aplikacja ma powtarzalny package dla Kubernetes.

**PASS:**

```text
✅ Helm chart istnieje w deploy/helm/demo-api
✅ Chart zawiera Deployment i Service
✅ image.repository i image.tag są sterowane przez values.yaml
✅ helm lint przechodzi
✅ helm template generuje poprawne manifesty
```

---

## Issue #7 — Add GitOps dev values

**Cel:** desired state środowiska dev jest w Git.

**Plik:**

```text
gitops/envs/dev/values.yaml
```

**PASS:**

```text
✅ values.yaml zawiera image.repository
✅ values.yaml zawiera image.tag
✅ values.yaml zawiera environment=dev
✅ Ten plik jest jedynym źródłem prawdy dla taga image w dev
✅ Zmiana image.tag powoduje zmianę desired state
```

---

## Issue #8 — Add kind local cluster bootstrap

**Cel:** projekt używa jednej decyzji: `kind`.

**PASS:**

```text
✅ Jest opis tworzenia klastra kind
✅ Nazwa klastra jest stała, np. gitops-cloud-lab
✅ README/Makefile nie mieszają kind z minikube/k3d
✅ kubectl get nodes pokazuje działający kind cluster
```

---

## Issue #9 — Install and configure Argo CD

**Cel:** Argo CD działa lokalnie i obserwuje repo.

**PASS:**

```text
✅ Argo CD działa w namespace argocd
✅ Argo CD Application jest w gitops/apps/demo-api-application.yaml
✅ Application wskazuje Helm chart i dev values
✅ App pokazuje Synced
✅ App pokazuje Healthy
```

---

## Issue #10 — Add GitOps update workflow with yq

**Cel:** po buildzie image workflow aktualizuje `image.tag`.

**Mechanizm:**

```text
GitHub Actions
→ yq update gitops/envs/dev/values.yaml
→ git commit
→ git push
```

**PASS:**

```text
✅ Workflow używa yq
✅ Workflow aktualizuje tylko image.tag
✅ Workflow robi commit np. chore: update demo-api image tag to sha-xxxx
✅ Commit trafia do main
✅ Nie ma Argo CD Image Updater
✅ Argo CD sam wykrywa zmianę z Git
```

---

## Issue #11 — Prove end-to-end GitOps deployment

**Cel:** pełny dowód v0.1.

**Scenariusz:**

```text
1. Zmieniasz kod /version
2. Push do main
3. CI przechodzi
4. Image trafia do GHCR
5. yq aktualizuje image.tag
6. Commit z image tagiem trafia do repo
7. Argo CD robi sync
8. curl /version pokazuje nową wersję/tag
```

**PASS:**

```text
✅ Przed zmianą /version pokazuje stary tag
✅ Po zmianie /version pokazuje nowy tag
✅ Argo CD pokazuje Synced + Healthy
✅ Całość działa w ~3 min
✅ Nie użyto ręcznie kubectl apply ani helm upgrade do deployu appki
```

---

## Issue #12 — Add rollback runbook by git revert

**Cel:** rollback jest przez Git, nie przez ręczne dłubanie w klastrze.

**PASS:**

```text
✅ docs/RUNBOOK_ROLLBACK.md istnieje
✅ Runbook pokazuje jak znaleźć commit z image.tag
✅ Runbook używa git revert
✅ Po revert Argo CD wraca do poprzedniego desired state
✅ curl /version pokazuje poprzedni tag
✅ Zero ręcznego rollbacku przez kubectl/helm
```

---

## Issue #13 — Add Terraform local skeleton

**Cel:** pokazać IaC minimalnie, bez chmury.

**Zakres:**

```text
infra/local/
```

**PASS:**

```text
✅ infra/local/providers.tf istnieje
✅ infra/local/main.tf istnieje
✅ terraform fmt przechodzi
✅ terraform validate przechodzi
✅ Terraform dotyczy tylko local setup
✅ Nie ma infra/aws/
```

---

# 5. Slice’y + PASS criteria 🧱

## Slice 0 — Foundation

**Issues:** #1

**Cel:** zamknąć decyzje i strukturę.

**PASS:**

```text
✅ Struktura repo v0.1 gotowa
✅ README gotowy
✅ ADR gotowy
✅ kind wpisany jako jedyny local cluster
✅ Brak infra/aws/
✅ Brak gitops/envs/prod/
```

---

## Slice 1 — Demo API

**Issues:** #2

**Cel:** appka pokazuje health i wersję.

**PASS:**

```text
✅ /health działa
✅ /ready działa
✅ /version działa
✅ Testy działają
✅ Brak /metrics
```

---

## Slice 2 — Docker

**Issues:** #3

**Cel:** appka działa jako image.

**PASS:**

```text
✅ docker build działa
✅ docker run działa
✅ /health działa z kontenera
```

---

## Slice 3 — CI

**Issues:** #4

**Cel:** GitHub Actions sprawdza testy.

**PASS:**

```text
✅ push/PR odpala CI
✅ testy przechodzą
✅ błędny test failuje workflow
```

---

## Slice 4 — GHCR

**Issues:** #5

**Cel:** image trafia do registry.

**PASS:**

```text
✅ image build działa w GitHub Actions
✅ image push działa do GHCR
✅ tag = sha-<commit>
```

---

## Slice 5 — Helm

**Issues:** #6

**Cel:** appka ma chart.

**PASS:**

```text
✅ helm lint działa
✅ helm template działa
✅ image tag jest parametryzowany
```

---

## Slice 6 — GitOps dev state

**Issues:** #7

**Cel:** `gitops/envs/dev/values.yaml` trzyma desired state.

**PASS:**

```text
✅ dev values istnieją
✅ image.tag jest w dev values
✅ środowisko dev jest jedyne w v0.1
```

---

## Slice 7 — kind + Argo CD

**Issues:** #8, #9

**Cel:** lokalny GitOps działa na kind.

**PASS:**

```text
✅ kind cluster działa
✅ Argo CD działa
✅ demo-api app w Argo CD = Synced
✅ demo-api app w Argo CD = Healthy
```

---

## Slice 8 — yq GitOps update

**Issues:** #10

**Cel:** workflow aktualizuje tag w Git.

**PASS:**

```text
✅ yq zmienia image.tag
✅ workflow robi commit
✅ workflow pushuje commit
✅ Argo CD widzi zmianę
```

---

## Slice 9 — E2E proof

**Issues:** #11

**Cel:** pełny flow działa od kodu do Kubernetes.

**PASS:**

```text
✅ code change przechodzi przez CI
✅ image trafia do GHCR
✅ GitOps values dostają nowy tag
✅ Argo CD syncuje
✅ curl /version pokazuje nowy tag
✅ brak ręcznego deployu
```

---

## Slice 10 — Rollback

**Issues:** #12

**Cel:** rollback przez `git revert`.

**PASS:**

```text
✅ git revert cofa commit z image.tag
✅ Argo CD syncuje poprzedni desired state
✅ /version pokazuje poprzedni tag
✅ runbook rollbacku jest gotowy
```

---

## Slice 11 — Terraform local

**Issues:** #13

**Cel:** minimalne IaC local-first.

**PASS:**

```text
✅ terraform fmt
✅ terraform validate
✅ infra/local działa jako skeleton
✅ cloud wraca dopiero w v0.4
```

---

# Po zakończeniu v0.1 — external goal 🎙️

To nie jest issue blokujące MVP. To jest trening rozmowy.

## Zadanie po MVP

Nagraj dla siebie **5-minutowe wyjaśnienie**:

```text
commit
→ CI
→ image
→ yq GitOps update
→ Argo CD sync
→ Kubernetes deploy
→ rollback by git revert
```

## PASS

```text
✅ Umiesz wyjaśnić flow bez notatek
✅ Umiesz powiedzieć, dlaczego kind zamiast minikube/k3d
✅ Umiesz powiedzieć, dlaczego yq zamiast Argo CD Image Updater
✅ Umiesz wyjaśnić rollback przez Git
✅ Umiesz powiedzieć, co świadomie wyciąłeś z v0.1
```

---

# Jedno działanie teraz

Zrób tylko **Slice 0 / Issue #1**:

```bash

git init

mkdir -p apps/demo-api/app apps/demo-api/tests
mkdir -p deploy/helm/demo-api/templates
mkdir -p gitops/apps gitops/envs/dev
mkdir -p infra/local
mkdir -p docs/ADR .github/workflows

touch README.md Makefile
touch apps/demo-api/README.md
touch deploy/helm/demo-api/Chart.yaml
touch deploy/helm/demo-api/values.yaml
touch gitops/apps/demo-api-application.yaml
touch gitops/envs/dev/values.yaml
touch infra/local/main.tf infra/local/providers.tf infra/local/variables.tf infra/local/README.md
touch docs/ARCHITECTURE.md docs/RUNBOOK_DEPLOY.md docs/RUNBOOK_ROLLBACK.md
touch docs/ADR/0001-local-first-kind-gitops.md
touch .github/workflows/ci.yml .github/workflows/image-build.yml .github/workflows/gitops-update.yml

git add .
git commit -m "chore: initialize gitops cloud lab structure"
```

**Najważniejsze:** v0.1 ma dowieźć jeden mocny obrazek w głowie:
**Git zmienia desired state → Argo CD dogania cluster.**
